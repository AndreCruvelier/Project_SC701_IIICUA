@model IEnumerable<CaprisMedica.UI.Models.Equipos>

@{
    ViewData["Title"] = "Index";
    Layout = "Layout2";
}

<div class="block-header">
    <h1>Lista de Equipos</h1>
</div>

<div class="row">
    <div class="col-xs-12">
        <button type="button" class="btn bg-grey waves-effect" onclick="location.href='@Url.Action("Create", "Equipos")'">Agregar Equipo</button><br /><br />
    </div>
</div>

<div class="row clearfix">
    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
        <div class="card">
            <div class="body">
                <div class="table-responsive">
                    <table class="table table-bordered table-hover js-basic-example dataTable" id="DatoEquipo">
                        <thead>
                            <tr>
                                <th>Id</th>
                                <th>Nombre</th>
                                <th>Estado</th>
                                <th>Acción</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.EquipoId)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.EquipoNombre)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.EquipoEstado)
                                    </td>
                                    <td>
                                        <a asp-action="Edit" asp-route-id="@item.EquipoId">Editar</a> |
                                        <a asp-action="Delete" asp-route-id="@item.EquipoId">Eliminar</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>